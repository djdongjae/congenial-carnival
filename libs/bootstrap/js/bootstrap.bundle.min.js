/* Bootstrap 5.3.0 - Minimal Version for Toast functionality */
(function(global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.bootstrap = {}));
})(this, (function (exports) { 'use strict';

  const Toast = class {
    constructor(element) {
      this._element = element;
    }
    
    static getInstance(element) {
      return element._toastInstance || null;
    }
    
    static getOrCreateInstance(element, config = {}) {
      return this.getInstance(element) || new this(element, typeof config === 'object' ? config : null);
    }
    
    show() {
      this._element.style.display = 'flex';
      this._element.classList.add('show');
      setTimeout(() => {
        this._element.classList.remove('show');
        this._element.style.display = 'none';
        this._element.dispatchEvent(new CustomEvent('hidden.bs.toast'));
      }, 3000);
    }
    
    hide() {
      this._element.classList.remove('show');
      setTimeout(() => {
        this._element.style.display = 'none';
        this._element.dispatchEvent(new CustomEvent('hidden.bs.toast'));
      }, 150);
    }
    
    dispose() {
      this._element._toastInstance = null;
    }
  };

  // Toast 초기화
  document.addEventListener('DOMContentLoaded', function() {
    const toastElements = document.querySelectorAll('.toast');
    toastElements.forEach(element => {
      element._toastInstance = new Toast(element);
    });
  });

  exports.Toast = Toast;
  exports['default'] = { Toast };

  Object.defineProperty(exports, '__esModule', { value: true });
}));
